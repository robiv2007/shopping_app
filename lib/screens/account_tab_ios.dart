import 'package:flutter/cupertino.dart';

class AccountTabIos extends StatelessWidget {
  const AccountTabIos({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Cart'),
    );
  }
}
