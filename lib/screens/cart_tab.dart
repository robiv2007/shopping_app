import 'package:flutter/cupertino.dart';

class CartTabIos extends StatelessWidget {
  const CartTabIos({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Cart'),
    );
  }
}
